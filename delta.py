import os
import subprocess
import sys

def main():
    if len(sys.argv) != 3: exit(1)

    set_size = sys.argv[1] # n, size of set to be minimized
    interesting_check = sys.argv[2] # command to check if set is interesting

if __name__ == "__main__":
    main()
